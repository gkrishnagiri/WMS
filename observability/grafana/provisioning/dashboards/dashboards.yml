apiVersion: 1

providers:
  - name: EOS dashboards
    orgId: 1
    folder: EOS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
